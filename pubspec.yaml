name: todo_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 2.2.0+1

environment:
  sdk: ^3.12.2

dependencies:
  window_manager: ^0.5.1
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  msix: ^3.17.0

flutter:
  uses-material-design: true

msix_config:
  display_name: Task Terminal
  publisher_display_name: Sparsh
  identity_name: com.sparsh.todo
  logo_path: windows/runner/resources/app_icon.ico
